[{kernel,
  [{distributed, [{ecomet_router, 3000, [erouter@localhost, {erouter1@localhost, erouter2@localhost}]}]},
   {sync_nodes_optional, [erouter@localhost, erouter2@localhost]},
   {sync_nodes_timeout, 5000}
  ]
 }
].
